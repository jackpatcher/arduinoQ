git init
git remote add origin https://github.com/jackpatcher/arduinoQ.git
git config --global user.name "jackpatcher"
git config --global user.email jack.patcher.social@gmail.com

git add .
git commit -m "XIN"
git push -u origin main